requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0
pytest>=7.4

# Optional: only needed if you set use_browser: true for a source
# (some sites render listings via JavaScript and need a real browser).
# After installing, run: playwright install chromium
playwright>=1.40
